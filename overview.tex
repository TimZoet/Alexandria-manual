%Type definitions and data stored in same database file.
%The \gls{library_class} class is the interface to an on-disk library file.
%Types can be defined using createType and createProperty methods on the library class.
%C++ classes corresponding to defined types can be defined using wrapper classes.
%ObjectHandler is interface to instance creation, updating, deletion, etc.